# Aeroelastic Flutter Simulation (C++)

Численное моделирование аэроупругого флаттера гибкого крыла с использованием собственного C++ solver-а, тетраэдральной сетки, reduced-order structural dynamics и VTK-визуализации.

---

# Демонстрация

![Flutter](docs/gif/flutter.gif)

---
# Возможности проекта

- Собственный aeroelastic solver на C++
- Reduced-order динамика конструкции
- Изгиб и кручение крыла
- Quasi-steady аэродинамика
- Аэроупругий флаттер
- Effective angle of attack model
- Unsteady aerodynamic lag
- Тетраэдральная FEM-сетка
- VTK / ParaView визуализация
- Расчет напряжений внутри tetrahedral elements
- Рост напряжений при флаттере
- Self-excited oscillations
- Поиск критической скорости флаттера

---

# Физическая модель

Проект реализует упрощенную, но физически осмысленную модель аэроупругого флаттера.

Цель модели:

- затухающие колебания при малой скорости потока;
- самовозбуждающиеся колебания около критической скорости;
- неустойчивый рост амплитуды при больших скоростях.

---

# Система координат

- **X** — направление потока
- **Y** — размах крыла
- **Z** — вертикальное направление

Набегающий поток:

$ \mathbf{U}_\infty = (U,0,0) $

Корневая секция крыла жестко закреплена.

---

# Модель конструкции

Крыло описывается reduced-order моделью с двумя степенями свободы:

- изгиб
- кручение

---

## Мода изгиба

Прогиб крыла:

$ w(y,t) = q_b(t)\phi_b(y) $

где:

- $q_b$ — обобщенная координата изгиба
- $\phi_b(y)$ — форма изгибной моды

Консольная форма:

$ \phi_b(\xi) = \xi^2(3 - 2\xi) $

где:

$ \xi = \frac{-y}{L} $

---

## Мода кручения

Крутка крыла:

$ \theta(y,t) = q_t(t)\phi_t(y) $

где:

- $q_t$ — обобщенная координата кручения
- $\phi_t(y)$ — форма крутильной моды

Текущая реализация:

$ \phi_t(\xi) = \xi $

или:

$ \phi_t(\xi) = \xi^2 $

---

## Кинематика малых углов

Кручение реализовано как линейная малоугловая ротация вокруг оси $Y$:

$ x' = x + z\theta $

$ z' = z - x\theta $

Приближение справедливо для малых деформаций.

---

# Динамика конструкции

Система уравнений движения:

$ M\ddot q + C\dot q + Kq = Q_{aero} $

где:

$ q =
\begin{bmatrix}
q_b \\
q_t
\end{bmatrix}
$

Используются диагональные reduced-order матрицы:

$ M =
\begin{bmatrix}
m_b & 0 \\
0 & I_t
\end{bmatrix}
$

$ C =
\begin{bmatrix}
c_b & 0 \\
0 & c_t
\end{bmatrix}
$

$ K =
\begin{bmatrix}
k_b & 0 \\
0 & k_t
\end{bmatrix}
$

Интегрирование по времени:

- semi-implicit Euler scheme

---

# Аэродинамическая модель

Используется quasi-steady аэродинамика.

---

## Динамическое давление

$ q_\infty = \frac12 \rho U^2 $

---

## Effective Angle of Attack

Главный механизм флаттера реализован через эффективный угол атаки:

$$ \alpha_{eff}
=
\theta
+
\frac{-\dot w + x\dot\theta}{U}
$$

Формула учитывает:

- геометрическое кручение;
- вертикальную скорость изгиба;
- скорость вращения профиля.

Это создает аэродинамическую связь между:

- изгибом;
- кручением;
- подъемной силой;
- аэродинамическим моментом.

---

## Unsteady Aerodynamic Lag

Для моделирования фазового запаздывания используется first-order lag model:

$$ \tau_a \dot\alpha_{lag}
+
\alpha_{lag}
=
\alpha_{eff}
$$

Подъемная сила рассчитывается через запаздывающий угол:

$ C_L = C_{L\alpha}\alpha_{lag} $

Это позволяет моделировать:

- phase lag;
- aerodynamic memory;
- отрицательное демпфирование;
- aeroelastic instability.

---

## Подъемная сила

Локальная подъемная сила:

$ L = q_\infty C_L A $

где:

- $A$ — nodal aerodynamic area.

---

## Аэродинамический крутящий момент

Момент относительно оси кручения:

$ M_y = -(x - x_{ac})L $

Именно этот момент связывает аэродинамику с torsion mode.

---

# Механизм флаттера

Флаттер возникает из-за замкнутой aeroelastic feedback loop:

```text
Скорость изгиба
    ↓
Effective angle of attack
    ↓
Изменение подъемной силы
    ↓
Аэродинамический момент
    ↓
Кручение крыла
    ↓
Дополнительное изменение подъемной силы
    ↓
Рост колебаний
```

При малой скорости:

$ C_{total} > 0 $

Колебания затухают.

Около критической скорости:

$ C_{total} \approx 0 $

Появляются устойчивые oscillations.

Выше скорости флаттера:

$ C_{total} < 0 $

Амплитуда начинает расти.

---

# Расчет напряжений

Напряжения рассчитываются внутри tetrahedral elements.

---

## Изгибные напряжения

$ \sigma = Ez\kappa $

где:

- $E$ — модуль Юнга
- $\kappa$ — кривизна

---

## Касательные напряжения при кручении

$ \tau = Gr\frac{d\theta}{dy} $

где:

- $G$ — модуль сдвига
- $r$ — расстояние до оси кручения

---

## Эквивалентное напряжение

Эквивалентное напряжение по Мизесу:

$$ \sigma_{eq}
=
\sqrt{\sigma^2 + 3\tau^2}
$$

Узловые напряжения вычисляются через усреднение по tetrahedral mesh.

---

# Визуализация

Проект экспортирует VTK-файлы для ParaView.

Доступные поля:

- displacement
- pressure
- effective angle of attack
- lift
- bending deformation
- torsional deformation
- equivalent stress

---

# Режимы работы

| Скорость потока | Поведение |
|---|---|
| Низкая $U$ | Затухающие колебания |
| Около $U_{crit}$ | Устойчивые oscillations |
| Большая $U$ | Divergent flutter |

---

# Численные особенности

Модель использует:

- линейную упругость
- small-angle approximation
- reduced-order structural dynamics
- quasi-steady aerodynamics

Модель намеренно упрощена для:

- вычислительной легкости;
- численной устойчивости;
- удобства расширения.

---

# Используемые технологии

- C++
- VTK
- ParaView
- Gmsh
- Tetrahedral FEM mesh

---

# Возможные улучшения

Потенциальные направления развития:

- нелинейная динамика
- full modal coupling matrices
- Theodorsen unsteady aerodynamics
- геометрическая нелинейность
- adaptive timestep
- разрушение конструкции
- multi-mode structural basis
- GPU acceleration

---

# Скриншоты

## Сетка

<p align="center">
  <img src="docs/images/mesh.png" width="800"/>
</p>

---

## Поле напряжений

<p align="center">
  <img src="docs/images/stress.png" width="800"/>
</p>

---

# Автор

Собственная реализация aeroelastic flutter simulation на современном C++ для исследований, визуализации и численного моделирования аэроупругости.